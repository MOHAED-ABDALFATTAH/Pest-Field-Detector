flask
pillow
requests

# Optional: PyTorch & Torchvision if you want to run the actual AI classification model.
# If these are not installed, the server will run in "Mock Mode" using mock predictions.
torch
torchvision
datetime
